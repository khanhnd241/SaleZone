
<!DOCTYPE html>
<html>
    <head>
        <h1><%= title %></h1>
    </head>
    <body>
        <form action="/update_user" method="POST">
            <% for (var i = 0; i < User.length; i++) { %>
            <div>
                <span>Name:</span><input type="text" name="name" value="<%= User[i].name %>">
            </div>
            <br>
            <div>
                <span>Phone:</span><input type="text" name="txt-phone" value="<%= User[i].phone %>" disabled><input type="text" name="phone" value="<%= User[i].phone %>" hidden>
            </div>
            <br>
            <div>
                <span>Roles:</span>
                <select name="roles">
                    <option value="" selected disabled hidden>Choose roles</option>
                    <option  value="admin">admin</option>
                    <option value="user">user</option>
                    <option value="mod">mod</option>
                </select>
            </div>
            <% } %>
            <br>
            <input type="submit" value="Save">
        </form>
        <form action="/" method="GET">
            <button>Home</button>
        </form>
    </body>
    
</html>